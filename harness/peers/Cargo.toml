[package]
name = "berm-peers"
description = "Name the other agents in the runtime — the smallest protocol harness"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[[bin]]
name = "peers"
path = "src/bin/main.rs"

[dependencies]
berm-lang = { workspace = true, features = ["protocol"] }
