# --emit-relocs keeps the relocations that identify indirect-call targets.
# Without it rvtime cannot tell which functions have their address taken.
[build]
target = "riscv64imac-unknown-none-elf"

[target.riscv64imac-unknown-none-elf]
rustflags = ["-Clink-arg=--emit-relocs"]
